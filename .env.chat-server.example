# chat-server 启动用（复制为 .env.chat-server，根目录 npm run dev:chat-server 会加载）
# 三 Agent 共用同一 secret；各 agent 的 .env / .env.agent2 / .env.gin 里 WEBCHAT_AGENT_SECRET 须一致

WEBCHAT_AGENT_USER_ID=kuroneko,shiro,gin
WEBCHAT_AGENT_SECRET=dev-shared-secret-change-me
